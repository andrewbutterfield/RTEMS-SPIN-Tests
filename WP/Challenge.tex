\section{The Challenge}

Partial Pre/Post-Conditions + C Tests to Formal API Semantics

\subsection{Example: Integer Div/Mod}

Laws of mod/div for integers in programming languages (e.g. Haskell):

\begin{eqnarray*}
   n \div (-d)    &=& -(n \div d)
\\ (-n) \div d    &=& -(n \div d)
\\ (-n) \div (-d) &=& n \div d
\end{eqnarray*}

\begin{eqnarray*}
   n \mod (-d)    &=& -(n \mod d)
\\ (-n) \mod d    &=& n \mod d
\\ (-n) \mod (-d) &=& -(n \mod d) 
\end{eqnarray*}

\def\abs#1{|{\!#1\!}|}
\def\sgn#1{\mathsf{sgn}~{#1}}

To reason about the above we also need functions for 
sign ($\sgn\_$)
and absolute values ($\abs{\_}$).

\begin{eqnarray*}
   \sgn n  &=& 1, \qquad n > 0
\\         &=& -1, \qquad n < 0
\\         &=& 0, \qquad \text{otherwise}
\end{eqnarray*}

\begin{eqnarray*}
   \abs n  &=& -n, \qquad n < 0
\\         &=& n, \qquad \text{otherwise}
\end{eqnarray*}

We assume that we know about integer arithmetic and comparisons.

We have preconditions regarding the ``zeroness`` of the divisor,
and the sign of both inputs. The sign-function can establish these.
We can express postconditions in terms of 
the absolute values and signs of the inputs.

We want a collection of disjoint and exhaustive
pre-conditions whose disjunction evaluates to true.

\newpage

\subsection{Specifying $\div$}

\subsubsection{Preconditions}

Those regarding $n$:
\begin{eqnarray*}
   \pre n &=& \setof{NZ,NP,NN}
\\ NZ &:& \sgn n = 0
\\ NP &:& \sgn n = 1
\\ NN &:& \sgn n = -1
\end{eqnarray*}

Those regarding $d$:
\begin{eqnarray*}
   \pre d &=& \setof{DZ,DP,DN}
\\ DZ &:& \sgn d = 0
\\ DP &:& \sgn d = 1
\\ DN &:& \sgn d = -1
\end{eqnarray*}

\subsubsection{Postconditions}

We assume function $\div'$ works correctly on positive numbers. 
\begin{eqnarray*}
   \post\div &=& \setof{UNDEFDIV,ISDIV,NEGDIV}
\\ n \div d &=& r
\\ UNDEFDIV &:& r = \bot 
\\ ISDIV    &:& r = \abs n \div' \abs d  
\\ NEGDIV   &:& r = -(\abs n \div' \abs d) 
\end{eqnarray*}

\subsubsection{Transitions}

\begin{eqnarray*}
   \pre{n}\times\pre{d} &\then& \post\div
\\ (\_,DZ) &\then& UNDEFDIV
\\ (NP,DP) &\then& ISDIV
\\ (NN,DP) &\then& NEGDIV
\\ (NP,DN) &\then& NEGDIV
\\ (NN,DN) &\then& ISDIV
\end{eqnarray*}

\newpage
\subsection{Testing $\div$}

Given a map from $(preN,preD)$ to $post$,
we generate test code that generates code to:
\begin{itemize}
   \item initialise $n$ and $d$ that satisfies $preN \land preD$
   \item run $\div$ with $n$ and $d$ as input, writing its results to $r$.
   \item check that the value of $r$ is correct.
\end{itemize}

\subsubsection{Test Example $()NN,DP$}

For $(NN,DP)\then NEGDIV$, 
$$\sgn n = -1 \land \sgn d = 1 \then r = -(\abs n \div' \abs d)$$
we generate (assuming $N,D \geq 1$):
\begin{eqnarray*}
   && n := -N ; d := D ; 
\\ && r := n \div d ;
\\ && test(r,-(\abs n \div' \abs d))
\end{eqnarray*}
The actual behaviour of $test(a,e)$ is
\begin{eqnarray*}
   && \ifC a = e \thenC \skipC \elseC logfail(a,e) \endC
\end{eqnarray*}
If we assume all tests pass, 
then here we can interpret it as a post-condition $r=-(\abs n \div' \abs d)$.

So, we want to compute the weakest precondition:
$$
  \wp(n := -N ; d := D ; r := n \div d ,r=-(\abs n \div' \abs d))
$$
noting that we have a pre-condition that says: $N \geq 1 \land D \geq 1$.

\begin{eqnarray*}
\lefteqn{\wp(n := -N ; d := D ; r := n \div d ,r=-(\abs n \div' \abs d))}
\EQm{\wp(S_1;S_2,R)}
\\&& \wp(n := -N,\wp(d := D ; r := n \div d ,r=-(\abs n \div' \abs d)))
\EQm{\wp(S_1;S_2,R)}
\\&& \wp(n := -N,\wp(d := D,\wp(r := n \div d ,r=-(\abs n \div' \abs d))))
\EQm{\wp(x:=E,R)}
\\&& \wp(n := -N,\wp(d := D,(r=-(\abs n \div' \abs d)[n \div d/r])))
\EQ{substitution}
\\&& \wp(n := -N,\wp(d := D,(n \div d = -(\abs n \div' \abs d))))
\EQ{$\wp(x:=E,R)$, substitution}
\\&& \wp(n := -N,(n \div D = -(\abs n \div' \abs D)))
\EQ{$\wp(x:=E,R)$, substitution}
\\&& (-N) \div D = -(\abs N \div' \abs D)
\end{eqnarray*}

% \newpage
% \subsection{Specifying \protect$\mod$}
% \subsubsection{Preconditions}
% Same as for $\div$.
% \subsubsection{Postconditions}
% We assume function $\mod'$ works correctly on positive numbers.
% \begin{eqnarray*}
%    n \mod d &=& m
% \\ UNDEFMOD &:& m = \bot 
% \\ ISMOD    &:& m = \abs n \mod' \abs d 
% \\ NEGMOD   &:& m = -(\abs n \mod' \abs d) 
% \end{eqnarray*}
% \subsubsection{Transitions}
% \begin{eqnarray*}
%    \_,DZ &\then& UNDEFMOD
% \\ NP,DP &\then& ISMOD
% \\ NN,DP &\then& ISMOD
% \\ NP,DN &\then& NEGMOD
% \\ NN,DN &\then& NEGMOD
% \end{eqnarray*}



